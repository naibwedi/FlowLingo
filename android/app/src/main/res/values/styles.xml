<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <style name="KeyboardKeyButton" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/keyboard_key_background</item>
        <item name="android:textColor">@color/keyboard_ink</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="KeyboardUtilityButton" parent="@style/KeyboardKeyButton">
        <item name="android:background">@drawable/keyboard_special_key_background</item>
        <item name="android:textColor">@color/keyboard_special_key_text</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="KeyboardActionButton" parent="@style/KeyboardKeyButton">
        <item name="android:background">@drawable/keyboard_action_background</item>
        <item name="android:textColor">@color/keyboard_action_text</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="KeyboardActionIconButton" parent="@android:style/Widget.ImageButton">
        <item name="android:background">@drawable/keyboard_action_background</item>
        <item name="android:tint">@color/keyboard_action_icon_tint</item>
        <item name="android:padding">12dp</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="KeyboardCompactActionButton" parent="@style/KeyboardActionIconButton">
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:padding">9dp</item>
    </style>
</resources>
